{
  "schema": "trench-engine-campaign-handoff-v2",
  "created_at": "2026-07-18",
  "workspace": "C:\\Users\\hooki\\df2-workstation",
  "title": "V1 feature-complete: locked state, signal chain, ship gate",
  "instructions_to_agent": [
    "Work only in C:\\Users\\hooki\\df2-workstation. Read AGENTS.md and FILTER_NOTEBOOK.md first.",
    "Every fact below is OBSERVED from code/tests as of commit e5015a08 unless marked otherwise. Verify against code before relying on line-level details.",
    "Nonlinear stages have no transfer function. Plots gate linear claims only; driven sound is gated by level-matched renders and Tyson's ear.",
    "One change per verdict. Build to a hearable render, deliver, wait.",
    "Renders must warm-start (hd_ab does a discarded full pass first). Level-match every A/B — the HD leveller alone is worth ~+0.7 dB."
  ],
  "locked_state": {
    "defaults": {
      "island_rate": "78125 Hz (hdMode=true DEFAULT). 39062.5 kept as compatibility fallback (hdMode=false). Bytes on disk are ALWAYS authored at 39062.5; HD is a decode-time re-derivation at load.",
      "slamDrive": "0.10 — output desk on by default, tame. 0.5 approved as 'insane' flavor, reachable by wheel.",
      "bite": "0.0 — the linear cascade, bit-exact. Bodies ship as authored; NO FILTER and utilities stay honest. Nonzero bite is per-preset seasoning or user's hand, NEVER a global default.",
      "keyTrack": "false (host param, no face control yet)",
      "amount": "1.0, morph 0.5, q 0.0, body 0 (NO FILTER), motion off, space 0"
    },
    "laws": [
      "NO NEW UI ELEMENTS — the face is locked (Tyson, 2026-07-18). Every feature must surface through existing controls (TYPE list, MORPH/Q wheels, AMOUNT, the MOVE chips, hidden host params) or per-body baked behavior. A feature that needs a new control does not ship until it doesn't.",
      "body240 format is immutable: 4 corners x 6 stages x 5 u16 words, authored at 39062.5. HD never touches disk bytes.",
      "The roster imprints TIMBRE, never harmony. No chord/scale bodies in the roster (Phrygian was built, gated clean, and pulled on this rule). Chord-body pipeline lives in dev/tmp/bytes_fix for a future on-demand catalog.",
      "Every option (bite, hdMode off, keyTrack, inputMode SLAM) must null bit-exact / byte-identical when disabled. All current ones are test-proven.",
      "PresetRoster.inc is never regenerated without re-appending the hand-added tail (demo six + De-Esser + De-Mudder).",
      "Gate chord/resonator bodies on DECODED POLE ANGLES (packed_probe -> acos), never on cascade magnitude maxima: half-step neighbours merge into one peak.",
      "interpolate_biquad returns BIQUAD rows -> evaluate with response::biquad_*_mag_db, never kernel_*.",
      "Coefficient-ramp convergence is geometric: probe runtime coefficients only after ~250k samples or mid-ramp values masquerade as math errors."
    ]
  },
  "signal_chain": {
    "plugin_pre": [
      "host input -> PluginProcessor.processBlock",
      "input envelope tap (dry): broadband peak, OR per-body tuned detector when a baked-react body is loaded (De-Esser HP 4k / De-Mudder LP 700, two cascaded one-poles, 4ms/140ms follower)",
      "KeyTrackDetector (if keyTrack): YIN on 4x-decimated ring, hop 512, pitch-class snap C-anchor +-6 semi, debounce 2, silence holds -> params.pitchRatio",
      "applyMotion: motion tiles / react push (baked-react bodies push Q with motion fully off) -> morph/q",
      "params: bite -> interstageDrive; ParamID::bite post-cascade biteDriveBlock held at 0"
    ],
    "island": [
      "FixedRateTrenchIsland.prepare(hostRate, blockSize, bridge, islandRate) — islandRate from hdMode at prepareToPlay; latency reported to host",
      "anti-fold guard: elliptic LP, stopband at islandRate/2, passband fold-2031.25 Hz, active only when hostNyquist > fold (bypassSRC when host==island rate)",
      "down-SRC 200-tap windowed sinc -> engine rate"
    ],
    "engine_per_sample (trench-core FilterEngine::process_sample_inner, order verified)": [
      "1. input stage: InputMode None (default) | MackieDeskSlam (into-filter desk, slam_drive ramped per control block; plugin scales param by kIntoFilterDriveScale=0.10) | Cvsd",
      "2. pre_drive_gain",
      "3. cascade L/R: 6 DF2T stages, per-sample coefficient ramp toward per-control-block targets (32-sample grid, phase-continuous). KEY TRACK transpose applied at interpolation (set_parameters: conjugate pairs root-factored, angle *= ratio clamped 0.5..2.0, radius kept, before AMOUNT taper; ratio 1.0 branches out bit-exact). AMOUNT taper after transpose. BITE: mackity_saturate (x - 0.1768 x^5) at the 5 inter-stage junctions, drive into curve g = 1 + 7*drive, unity-compensated, per-sample ramped; drive 0 branches to the untouched linear loop",
      "4. AGC: table leveller (agc_drive=2.0 pre-scale law), active_agc_table(rate) — >65k uses the sqrt table (the DLL's own rate law, so HD gets authentic successor behavior)",
      "5. output gain (interpolated body boost, ramped)",
      "6. DC blocker (~20 Hz)",
      "7. saturate() output soft-knee (safety, transparent below knee)"
    ],
    "engine_per_block_post": [
      "QSound spatial (SpatialMode::QSound when space > 0): measured re-capture, shelf corners 513/1816 Hz, width guard keeps lows mono (process_block tail, engine.rs ~808)"
    ],
    "plugin_post": [
      "up-SRC back to host rate",
      "biteDriveBlock (post-cascade grit) — held at 0",
      "MOVE guard duck (up to -5 dB at gesture peak)",
      "output gain (dB param)",
      "SLAM: Mackie output desk (Airwindows Mackity model) at host rate, applied ONCE, final audible operation. Default drive 0.10"
    ]
  },
  "verified_this_build": [
    "tests green: 22 suites. Key contracts: bite_interstage (drive-0 bit-exact, harmonics live, aliasing folds -62..-75 dBc at 39k), key_tracking (ratio-1.0 bit-exact; commanded 1.5 measured 1.49966, radius preserved), hd_island (native-rate verbatim; TF overlay <=0.233 dB below 12k; 12-15k diverges <=0.43 dB = base-rate Nyquist cramping relaxing; ring tau in seconds preserved; SCALE re-derived by per-stage DC match — 'SCALE unchanged' was measured wrong by 34 dB)",
    "island SRC sweep-null (dev/tmp/island_null/): HD default = 0.00 dB at 15/18/19.5k all host rates, passband residual -21 dB, ripple 0.17 dB. Legacy island ceiling was ~17.5k. Chain gain +2.37 dB (HD AGC table) — level-match A/Bs",
    "De-Esser (5/7/9k notches) + De-Mudder (150/250/400<->300/450/700 via MORPH): Q0 pose exactly flat (0.0000 dB, pole-zero word-level cancellation), auto-react baked with tuned detectors, in roster and installed",
    "KeyTrackDetector standalone test: all pitch classes exact, octave fold works, silence holds (dev/tmp/keytrack_test/)",
    "installed C:\\Program Files\\Common Files\\VST3\\TRENCH.vst3 == build artifact (hash-verified at every install this session)"
  ],
  "unverified_ear_debt": [
    "FL pass on EVERYTHING at the new defaults (HD island + SLAM 10%): all presets re-derive at 78125 and were never re-auditioned. Cue known bodies (Rubber Mouth) for the leveller feel.",
    "De-Esser on a real vocal; De-Mudder on a real mix; Key Track with the LIVE YIN detector in FL (engine half proven, detector proven synthetic, never together in-host)",
    "BITE render pack across bodies beyond Talker (FUZZ_B_RAZOR / Rubber Mouth / Tooth Field at drives 0/.2/.5/.8) if the taste point needs a second opinion"
  ],
  "next_missions_ranked": [
    {
      "id": "SHIP",
      "note": "FL ear pass -> roster freeze (audition page) -> FL 150% DPI + wheel-feel QoL check -> demo video (shot list proven: reese riff -> key track -> bite -> slam -> orbit; harness renders in dev/tmp/hd_ab) -> package. All verdicts, no builds."
    },
    {
      "id": "RACK",
      "note": "Two bodies in series (12 biquads, runtime headroom proven). Utility-into-character in one instance. Zero format changes. First build after ship. Surface without new UI: rack pairs are PRESETS in the TYPE list (e.g. 'De-Essed Talker'), not a second selector."
    },
    {
      "id": "G_z_cube",
      "note": "Body-to-body morph. z_crossfade implemented+tested in minifloat.rs; needs engine plumbing (two cartridges + z param) + surface. The preset list becomes a space. Surface without new UI: a z-cube pair IS a preset; MORPH is the travel axis."
    },
    {
      "id": "F_audio_rate_morph",
      "note": "Scouted: per-sample lerp vs 32-grid measured -32.6 dB (grid vindicated for control-rate); the prize is RATE:AUDIO anatomy-FM (render dev/tmp/hd_ab/reese_ps_fm_48k.wav). Build only on ear verdict."
    },
    {
      "id": "SAUCE_per_body",
      "note": "Approved direction, table-driven via bodyBakedReactSpec pattern, zero UI: stereo bloom (L/R cent offset via pitch_ratio), react->MORPH axis option, baked keyTrack per body, per-body bite/space seasoning. Tuned-lock (12-TET snap at load) same trick as HD re-derivation."
    },
    {
      "id": "KEYTRACK_SURFACE",
      "note": "Key Track must surface with NO new UI: per-body baked (bodyBakedReactSpec pattern — tracking bodies simply track) and/or the hidden host param. No face control, ever."
    },
    {
      "id": "CATALOG",
      "note": "On-demand chord/scale body generation (user picks root+quality; compile through the proven gates). NOT roster presets — restraint law."
    }
  ],
  "beyond_body240": {
    "context": "Runtime headroom is proven far beyond one cascade (~100 biquads is comfortable). Two lanes: COMPOSE existing 240-byte bodies at runtime (no format change), or EXTEND the format (V2). The 6-stage body240 itself stays immutable either way.",
    "compose_no_format_change": [
      {
        "id": "RACK",
        "note": "Two bodies in series (12 biquads): utility-into-character in one instance (De-Esser -> Talker). One hidden second slot. Highest usability-per-line-of-code."
      },
      {
        "id": "Z_CUBE",
        "note": "Body-to-body morph (12 biquads): MORPH travels BETWEEN two loaded bodies via z_crossfade (implemented+tested in minifloat.rs). The preset list becomes a space."
      },
      {
        "id": "UNISON_WALL",
        "note": "3-5 detuned copies of one body (24-60 biquads), cents-spread, summed — the filter supersaw. Stereo bloom (L/R cent offset via existing pitch_ratio machinery) is the 2-voice starter."
      }
    ],
    "extend_format_v2": [
      {
        "id": "MEASURED_OBJECTS",
        "note": "A bodyN format (e.g. 12-24 stages/corner, 'body960') fits REAL measured objects — guitar body, cymbal, cab, room modes — as morphing cascades. 'Put your drums inside a grand piano', from measurements. The whole authoring/certify/decode pipeline generalizes; only the byte layout and grid-certification budget change."
      },
      {
        "id": "HRTF_ORBIT",
        "note": "Measured-ear spatial: fit HRTF magnitude trajectories (SONICOM, 403 subjects, already on disk at dev/tmp/hrtf/sonicom) as morphing cascades — ORBIT graduates from psychoacoustic QSound to real elevation/azimuth. Needs the bodyN lane above."
      },
      {
        "id": "MULTIBAND",
        "note": "Crossover split, one body per band (3x6 biquads + guards): mud body on lows / character mids / open top — TRENCH as a master-bus device. Furthest from current product shape; park last."
      }
    ],
    "restraint": "None of these enter V1. RACK and Z_CUBE are the first two post-ship builds; format extensions wait for a V2 decision. Catalog-lane chord generation (see next_missions_ranked) composes with all of them."
  },
  "artifacts": {
    "render_harness": "trench-core/src/bin/hd_ab.rs (cargo bin hd-ab): island A/B, reese riff w/ key track, fx modes inslam/outslam/orbit, per-sample probes, warm-start. Env: HD_AB_SOURCE=reese, HD_AB_KEYTRACK=1, HD_AB_FX=..., HD_AB_SLAM=, HD_AB_MORPHSTEP=1, HD_AB_PERSAMPLE=lerp32|lerp1|fm",
    "renders": "dev/tmp/hd_ab/*.wav (48k deliverables resampled 384/625 from 78125)",
    "measurements": "dev/tmp/island_null/ (SRC sweep-null, results.md), dev/tmp/bytes_fix/*_tf.png",
    "body_builders": "dev/tmp/bytes_fix/build_de_esser.py, build_utilities.py (hum surgeon / de-harsher / presence riser authored+gated, NOT shipped; presence riser needs HF renorm), build_phrygian.py (pulled body, catalog-lane evidence)",
    "unshipped_bodies": "dev/tmp/bytes_fix/DEMO_hum_surgeon|de_harsher|presence_riser|phrygian.body240"
  }
}