[package]
name = "trench-core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
libc = "0.2"

[features]
packed_interp = []
serde = []

[dev-dependencies]
hound = "3"
