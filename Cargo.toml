[workspace]
members = ["trench-core"]
resolver = "2"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

# `package."*"` skips workspace members. Keep the packed runtime and audio
# engine optimized in development builds.
[profile.dev.package.trench-core]
opt-level = 3
