// painter — the drawing vocabulary (no product semantics).
//
// theme is the single source of color/type truth; layout, hit lists and
// paint primitives migrate here from the monolith as bites touch them.

pub mod theme;
