// sources — the corpus domain: the START manifest (four provenance lanes)
// and its sidecar payloads. No drawing in here; no file IO outside of here
// (paint code consumes only what the catalog loaded).

pub mod manifest;
