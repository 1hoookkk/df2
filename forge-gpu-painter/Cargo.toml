[package]
name = "forge-gpu-painter"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = { version = "0.29", features = ["wgpu"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
cpal = "0.15"
bytemuck = { version = "1", features = ["derive"] }
sha2 = "0.10"
trench-core = { path = "../trench-core" }
