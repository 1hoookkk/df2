# Incomplete prior-run salvage

The prior `xhigh` run failed at the API TPM gate before producing a report.
Treat these as unfinished leads, not doctrine:

- Make the packed shipping path the only validation owner. Avoid a parallel
  Python packed-bilinear implementation.
- Define explicit hard gates for finite output, stability, unity DC, headroom,
  sub pedestal, collision/fusion diagnostics, and held-out middle states.
- Validate endpoints plus a dense `MORPH x SECONDARY` grid; inspect packed pole
  and zero trajectories slot-by-slot.
- Keep frequency placement deterministic and acoustic/perceptual: table-driven
  vocal formants, tube laws, modal ratios, Bark-aware checks, and no coordinate
  jitter.
- Preserve stage-slot identity across the four corners.
- Treat close resonances as an intentional-or-reject decision, not a blanket
  ban: accidental fused humps need compensation or rejection.
- Rewrite or replace the current static-corner authoring script with one owner
  for a joint six-cavity body specification.
- Worked examples are proof fixtures only. Human taste remains downstream.

Finish the requested constitutional algorithm. Correct any bad lead above.
