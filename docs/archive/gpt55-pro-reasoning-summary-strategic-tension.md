**Exploring remote-zero counterweight library**

I'm thinking about how the remote-zero counterweight library is important for managing broad tension without copying references. I want to experiment by adding an analytic remote-zero macro to shelf or window constructors and testing it with a before-and-after plot contact sheet—also watching for any sub-200 warnings. For the packed-middle QA, I plan to sample a 5x5 grid to reject any unintentional blow-up. Then, I'll explore short-form content capturing recipe steps and reactions. Time to build the foundation and controls!

**Simplifying row allocation**

I need to keep this straightforward! For row allocation, I'm considering six lane labels and whether active rows should be fixed by the recipe. Maybe I can include visible controls like a row lane list with grammar badges and solo plots. The visible controls might also feature elements like Recipe, Anchor, Intensity, Morph Travel, and Secondary Stress. I want to avoid using a full root editor while ensuring plot outputs reflect both magnitude and phase. It sounds like both numerator and denominator are essential, so I won't omit zeros.

**Clarifying plot outputs and constructors**

For the plot outputs, I want to capture the full packed-runtime magnitude response across the four corners, the center, and two contact strips. Maybe I could visualize this with a heatmap for S and M sweeps? It’s essential to include overlays that highlight numerator and denominator contributions, zero/pole locations, and any warnings about stability. 

For the constructor subset, I’ll have three deterministic options: descending cliff, rising edge, and opposed window, potentially with an optional remote-zero counterweight. I think I’ll refine the first build to focus on these elements while ensuring the remote-zero option is included.

**Defining the acceptance process**

I'm thinking about accepting candidates without any vocal mountains, pipe, or object modes in the first build—maybe I'll include those later. For the acceptance gate, the author should create ten candidates within one afternoon, but I want clarity on the criteria. 

The author must produce two original four-corner bodies in 90 minutes, ensuring all 24 row states are valid. The gate needs to include specifics, like exporting one 240-byte body with a JSON recipe, generating plots from packed-domain interpolation, and allowing the author to mark candidates after tests. If they can’t make two edits based on plots before listening, then they'll fail.

**Focusing on acceptance criteria**

I need to highlight acceptance without getting bogged down in unnecessary building phases yet. I won't include things like a full root-domain editor, a random root/batch AI generator, or vendor reference rankings at this stage. It's crucial to keep the focus tight—no collapsing of the MD and sealed ROM in the text. 

I must be clear: starting with a root editor might lead to inefficient time spent. The first build should prioritize creating a foundation-first, Morph-Designer-like composer that is response-led, row-aware, and deterministic, while avoiding concepts like "broad roadmap" for now.

**Evaluating editor design decisions**

I’m thinking about whether to dive into a "full six-row four-corner root-domain editor" or opt for a more controlled approach. It seems wiser to use a composer with "surgical row repair" instead, focusing on small, high-leverage directions. 

I can include a root-domain view as an underlay without full control and allow macro nudges after plot failures. The original bodies could still function with a sealed ROM. The program composer should expand into a four-corner ROM-class body, revealing six row lanes without hiding them. Finally, I want visible controls that show which constructor owns each row while keeping coefficients restricted.

**Setting guidelines for design and documentation**

I’m realizing I don’t want to design every single screen or get stuck in a month-long blank-canvas coefficient loop. The root-domain editor needs to be our starting point, and I want to emphasize strict clean-room provenance. This means including a recipe provenance file with clear laws and parameters, avoiding vendor curves.

For section one, I need to keep it under 900 words, aiming for about 600, and make sure the title and sections are clearly defined without any extra subsections. In the First Build, I can use bullets for "visible controls" and keep focused on using lawful raw material and sanitized behavioral evidence. Let’s aim for a final product that is detailed but compact!

**Clarifying guidelines for the project**

I need to ensure I avoid vague language, like saying "explore further" without specifying a bounded experiment. Instead, I'll use "test by..." explicitly. I’ll mention that random roots are rejected and clearly state there will be no copying of vendor data. 

It's essential to avoid relitigating runtime or inventing a hidden third axis. I won’t let AI approve taste since the author must make selections. Lastly, I'll include specific warnings for sub-200 issues and ensure acceptance criteria include inspecting sampled states from the 5x5 grid before listening.

**Finalizing project guidelines**

Alright, I’m confirming no random poles or zeros; only deterministic analytic constructors are allowed. I should mention the "full six-row four-corner root-domain editor" in the "Do Not Build Yet" section and the verdict as well. For the strategic opportunities section, I need exactly five, each described in two sentences.

I have to be careful about "Visible controls" to avoid overcrowding. In the acceptance gate for the "First Build," it must be explicit: 

- A clean session should produce 12 deterministic candidate contact sheets from three recipes and export at least one valid 240-byte body within 90 minutes.
- All plots must show 4 corners, center, a 5x5 heatmap, sub-200 warnings, pole stability, and overlay of zeros.
- The author should be able to name the intended Morph story and Secondary stress from the plots prior to hearing it and then perform one manual accepted edit.

**Refining project criteria**

I'm thinking we should have "at least one keeper candidate" judged by the author after listening, not by the tool. The manual accepted edit can use a row macro nudge instead of going through the full root approach. If that’s not met, it should fail. I should mention that the constructor subset must be compiled into the same runtime banks, but without duplicating MD across secondary constructs. 

There's a hidden risk if the composer ends up being one-dimensional, which would be a failure. The section verdict needs to clarify that this isn't a historical Morph Designer clone; each constructor must create four distinct corners by default, even if they are symmetric. For the strategic opportunities, I should present them as bounded experiments and have a short content surface for clarity.
