---
name: df2-operator
description: Thin router for DF2 plugin, Forge, corner quarry, cartridge, DSP, UI, and build work. Use whenever the user asks about df2, Trenchwork, bodies, corners, cartridges, the Forge, trench-core, or the JUCE player.
---

# DF2 Operator

This skill is a router, not a knowledge base.

Read only:

1. `CLAUDE.md` for the operating contract.
2. `gpt55-pro-report-destruction.md` for body representation and authoring workflow.
3. `CORNER_QUARRY_TRUTH.md` for original static-corner generation and mining.
4. The live code involved in the requested task.

Use fresh tests, plots, renders, and listening checks to resolve implementation
questions. Do not create or revive worklogs, handoffs, doctrine files, taxonomies,
session logs, or speculative specs.

When evidence conflicts:

1. Tyson's current direct instruction wins.
2. The two truth documents define desired direction.
3. Live code defines current implementation reality.
4. Fresh command output, plots, and shipped-engine audio determine whether a
   proposed change works.

Keep clean-room study material under `ref/` study-only. Never ship third-party
coefficients, names, extracted tables, or reverse-engineered artifacts.
