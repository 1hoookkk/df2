target/
dev/
data/*
!data/.gitignore
.venv/
wandb/
juce-shell/build/
__pycache__/
*.pyc
.DS_Store
Thumbs.db

# --- build artifacts / vendored deps / scratch (forge-recovery lock-in) ---
target_vst_static/
# stray Cargo target variants (e.g. target_alt_*) — build output, never commit
target_alt*/
juce-shell/JUCE/
.playwright-mcp/
*.7z
*.zip
*.exe
# AI-generated scratch images dropped into the asset tree
juce-shell/assets/images/ChatGPT Image*.png
# local/personal Claude config (machine-specific; not shared)
.claude/settings.local.json
.claude/*.lock
# reproducible, seeded body search output (regenerable; not source of truth)
bodies/generated/
# loose diagnostic screenshots / demo clips dropped at repo root
/*.png
/*.mp4
# desk judgment sheets: regenerable bulk (plots/wavs); the bank + ledgers ARE tracked
desk/sheets/

# Trace generated map
.trace/
