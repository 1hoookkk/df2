# BANK v1 — the kept bodies

The product. A body enters this ledger only with Tyson's verdict attached.
Each keeper gets: `<name>.body240` + `<name>.cart.json` + `<name>.png` in this
folder, a row here, then export + 17×17 packed audit + JUCE roster wiring.

| # | name | source (wave/rank/slug) | Tyson's words | date | audit |
|---|------|-------------------------|---------------|------|-------|
|   |      |                         |               |      |       |

<!-- Claude fills rows as verdicts arrive. Names are clean-room originals —
     never reference preset names. -->
